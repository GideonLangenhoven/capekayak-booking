import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Gift Voucher | Book Your Tour",
};

export default function VoucherLayout({ children }: { children: React.ReactNode }) {
  return children;
}
