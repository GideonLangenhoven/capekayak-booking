import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Voucher Purchased | Book Your Tour",
};

export default function VoucherSuccessLayout({ children }: { children: React.ReactNode }) {
  return children;
}
