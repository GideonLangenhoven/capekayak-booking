import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Voucher Confirmed | Book Your Tour",
};

export default function VoucherConfirmedLayout({ children }: { children: React.ReactNode }) {
  return children;
}
