import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Book a Tour | Book Your Tour",
};

export default function BookLayout({ children }: { children: React.ReactNode }) {
  return children;
}
