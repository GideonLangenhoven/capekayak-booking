import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Payment Cancelled | Book Your Tour",
};

export default function CancelledLayout({ children }: { children: React.ReactNode }) {
  return children;
}
