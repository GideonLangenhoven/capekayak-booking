import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Sign Waiver | Book Your Tour",
};

export default function WaiverLayout({ children }: { children: React.ReactNode }) {
  return children;
}
